# Bravo Data Extraction — KNOWN ISSUES (READ THIS FIRST)

## 🚫 SIGN INTO / RECOVER BRAVO WITHOUT PARALLELS — DO THIS, every time

If any Bravo pull stalls — trigger sits in `triggers/` or `triggers/claimed/` >2 min unprocessed, an all-cells-error result, or no output CSV after the expected time — Bravo is wedged or sitting at a login screen. **Recover it PROGRAMMATICALLY. Do NOT request Parallels access, do NOT drive the Parallels GUI with computer-use, do NOT ask Joshua to sign in.** The entire pipeline is "no Parallels grant required" by design.

**The mechanism (canonical — used by `daily-funds-verification`, `monday-bravo-combined-run`, `funds-verification-watchdog`):**
`prlctl exec` HANGS from an interactive osascript session but runs cleanly from a **scheduled-task session**. So fire a ONE-SHOT scheduled task (`mcp__scheduled-tasks__create_scheduled_task`, `fireAt` ≈ 60 s from now, taskId like `bravo-recover-oneshot-<timestamp>`) whose prompt runs ONE of:

- **Watcher only** (watcher hung/dead, Bravo still logged in on a Dashboard):
  `osascript do shell script "/usr/local/bin/prlctl exec '{7dc84f03-4e68-4f43-9596-bf8a7dfb8e0a}' --current-user powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'Y:\Documents\Claude\Projects\Bravo Data Extraction\_restart_watcher.ps1'"`

- **Full relaunch** (Bravo at a login screen, or not running — this launches Bravo via the `.appref-ms` shortcut + `BravoAutoLogin` AND restarts the watcher):
  `osascript do shell script "/usr/local/bin/prlctl exec '{7dc84f03-4e68-4f43-9596-bf8a7dfb8e0a}' --current-user powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'Y:\Documents\Claude\Projects\Bravo Data Extraction\_relaunch_bravo_and_watcher.ps1'"`

**Then:** wait ~90 s; confirm recovery with
`prlctl exec '{7dc84f03-4e68-4f43-9596-bf8a7dfb8e0a}' --current-user tasklist /v /fi "IMAGENAME eq Bravo.exe" /fo list` → `Status: Running` (not "Not Responding"), and `logs/watcher.last_started.txt` timestamp advanced. Then re-drop your trigger and resume polling.

**Rule for every Bravo-touching task/session:** stalled pipeline → fire the one-shot recovery above → re-drop → continue. Never Parallels GUI. Never ask Joshua to log in. (Restart is TRIAGE to clear a wedge so the pull can run — it is not a substitute for the closing-handler Continuous-Scrolling fix below.)

---

**Purpose:** Read this file BEFORE doing any Bravo combined-run, closing-report, or "Not Responding" work. Do not re-investigate settled facts. Append new confirmed findings; never delete history. Last updated: 2026-06-07.

---

## ⛔ TOP RULE
Killing/restarting Bravo is TRIAGE ONLY, never "the fix." The fix is to stop the automation from inducing the hang. Do not propose kill-and-rerun as a solution.

---

## ✅ CONFIRMED ROOT CAUSE — "(Not Responding)" is induced by OUR automation
Bravo NEVER hangs when Joshua runs reports by hand. The automation does something Joshua never does: **it manipulates the Report Preview "Enable Continuous Scrolling" toggle on every closing/journal report.**

- That toggle forces Bravo to render the ENTIRE multi-page report as one giant canvas. On wide closing reports (End of Month, etc.) this pins Bravo's UI thread → Windows marks it **"(Not Responding)"** for minutes. (Documented in the handler's own comments.)
- The toggle-state detection is fragile (button is labeled by action "Enable Continuous Scrolling", not state). History shows it was rewritten multiple times on 2026-05-29 (`.bak-pre-toggle-fix`, `.bak-pre-size-threshold`) and still fails — so the automation often ends up turning the freeze-inducing mode ON rather than OFF.
- The automation then fires UIA queries (ClickByName Export, FindByName) AT the frozen/rendering window with tight timeouts, which piles cross-process calls on a blocked UI thread and makes the wedge permanent.
- **Why Joshua never sees it:** he opens the paginated preview, looks at page 1, clicks Export → CSV. He never touches Continuous Scrolling. CSV export captures the full report REGARDLESS of view mode, so the toggle is both unnecessary AND the cause.

### Handlers that contain the toggle code (the suspects):
EndOfMonth, EndOfDay, EndOfDayConsolidated, DepositsAndPaidOuts, DisbursementJournal, GeneralException, InterStoreCashTransfer, LargeCashTransactions, Transfers.
(All in `reports/`. The `aged-inventory / loans-75 / layaways / employee-activity / chekkit-inactives` reports do NOT toggle CS — they only fail because a prior closing-report hang already wedged Bravo.)

---

## ✅ CONFIRMED CASCADE
Once Bravo is wedged on a Report Preview, `EnsureStore` → `SwitchStore` → `BackToDashboard` can't reach Dashboard or find "Lock Session", so EVERY later store fails ("EnsureStore failed"). The whole combined run returns `partial` with all cells errored. The watcher logs `cause=nav — NOT a lockout risk; breaker not incremented`, so it never escalates or self-heals — it just burns ~45s/cell failing, then the compile step has nothing to post. **This is why the Monday combined analysis has never completed.**

---

## 🔧 PERMANENT FIX PLAN (status tracked here)
1. **[CORE] Remove the Continuous Scrolling toggle manipulation** from all 9 closing/journal handlers. Export straight from the default paginated preview (mirror Joshua's manual path). — STATUS: pending Joshua go-ahead
2. **Don't fire UIA at a rendering/hung window.** Gate ribbon interactions on a responsiveness check (SendMessageTimeout SMTO_ABORTIFHUNG / IsHungAppWindow); wait for render to finish like a human. — STATUS: pending
3. **Always close Report Preview on exit AND on error; verify Dashboard before returning** so a failure can't cascade. — STATUS: partial (recovery block exists, still cascades)
4. **Preflight responsiveness gate** in monday-bravo-combined-run: check `tasklist /v` Status != "Not Responding" AND on Dashboard before dropping triggers; recover first if not. — STATUS: pending
5. **Fail loud:** compile task DMs Joshua if >25% cells error instead of posting nothing. — STATUS: pending
6. **Fail fast:** abort run after first store's EnsureStore fails twice (don't burn 19 min). — STATUS: pending

Roll-out rule: back up each file, edit ONE handler (EndOfMonth), smoke-test ONE cell, confirm CSV written + Bravo stays responsive + returns to Dashboard, THEN propagate to the other 8.

---

## 📋 RUN HISTORY
- **2026-06-07:** Combined run failed 25/25 cells. Bravo was ALREADY "(Not Responding)" on a HAR End-of-Month Report Preview (6/1 data) from a prior closing-report run before triggers were even dropped. Confirmed root cause above via code read (`reports/EndOfMonth.ahk`, `bravo_export.ahk`). Created this log.

## 🔑 ENV FACTS
- Parallels VM GUID: `{7dc84f03-4e68-4f43-9596-bf8a7dfb8e0a}`
- Project root (Mac): `/Users/joshuadavis/Documents/Claude/Projects/Bravo Data Extraction`  (= `Y:\Documents\...` inside VM)
- Watcher: `bravo_watcher.ahk` (PID varies). Auto-login: `BravoAutoLogin.ahk`.
- Check Bravo responsiveness: `prlctl exec '{GUID}' --current-user tasklist /v /fi "IMAGENAME eq Bravo.exe" /fo list` → look at `Status:` field ("Running" vs "Not Responding").

## ✅ CONFIRMED 2026-06-07 — auto-login does NOT launch Bravo
`BravoAutoLogin.ahk` only fills the login form (on Ctrl+Shift+L / when the Bravo login window is active). It does NOT start the Bravo process. So after a kill, Bravo stays down until relaunched manually:
- Launch via ClickOnce ref: `C:\Users\joshuadavis\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Bravo Store Systems\Bravo.appref-ms`  (cmd: `start "" "<that path>"`)
- The `monday-bravo-combined-run` SKILL preflight wrongly assumes auto-login relaunches Bravo — it doesn't. Triage steps must explicitly relaunch Bravo, then let BravoAutoLogin fill creds.

## ✅ CONFIRMED 2026-06-07 (b) — headless re-login is unreliable; do NOT kill Bravo
After killing Bravo, getting it logged back in headlessly failed repeatedly. Root reason: BravoAutoLogin only acts when `WinActive("Bravo ahk_class")` is true, but external `WinActivate` from prlctl-launched AHK does NOT reliably bring Bravo to the foreground in the VM, and every `prlctl exec` of a console program (cmd/powershell/tasklist) spawns a Windows Terminal that grabs foreground. Net: Bravo sits at the login screen, unfocused, and never auto-logs-in.
- LESSON: treat "always logged in" as an invariant. Do NOT kill Bravo as triage. If Bravo is ever killed/closed, the fastest recovery is a human clicking the Bravo login once (or driving Parallels visually via computer-use), NOT headless keystrokes.
- The watcher (in-session) keeps Bravo foreground during normal store-switching, which is why auto-login works in production but not for external recovery.

## ✅ FIXES SHIPPED 2026-06-07
1. **CORE: Continuous Scrolling toggle REMOVED from `reports/EndOfMonth.ahk`** (backup `.bak-pre-cs-removal-2026-06-07`). 0 toggle refs remain; export path intact. This is the fix for the recurring "Not Responding" hang. STILL the other 8 closing/journal handlers to do AFTER a clean smoke test.
2. **`lib/StoreCycle.ahk` EnsureStore: added Select Store selector handling** (backup `.bak-pre-selector-fix-2026-06-07`) — on the Global Access store selector it now double-clicks the target store row, THEN RecoverFromAutoLock. PROVEN working in logs ("selector -> double-click 'Harrisonburg'", "store confirmed: HAR").
3. **Self-heal recovery script** `_recover_to_dashboard.ahk` — from Select Store/login → dashboard, verifies the "Reports" sidebar. Run in-VM: `AutoHotkey64.exe _recover_to_dashboard.ahk HAR`.

## ✅ NEW FINDINGS 2026-06-07 (render + bounce)
- **Freshly-relaunched Bravo renders BLACK** (window exists, title shows store, but client area unpainted; UIA finds no "Reports"). **Maximizing/activating the window wakes the renderer** (`WinRestore`+`WinActivate`+`WinMaximize` → Bravo splash paints). Root cause of the "black window" seen all session. ActivateBravo should probably maximize too.
- **Login BOUNCE/auto-lock:** after a successful Submit (onLogin=no, code=HAR, splash paints), Bravo drops BACK to the login screen within ~1 min idle. This makes cold-start login flaky and, with repeated retries, risks an account lockout (see 2026-05-13). DO NOT hammer logins.
- **`LogVisibleNames` HANGS** on the login/selector screen (UIA full-tree enumerate). Avoid it there; use bounded `ExistsByName`/`WaitForAnyByName`.
- The login screen's "Close" element is the **app window close** — clicking it QUITS Bravo. Never click "Close" to dismiss.

## 🚑 SAFE RECOVERY ORDER (after a kill, to avoid lockout)
1. Ensure Bravo running & rendered: launch via `Bravo.appref-ms`; then maximize/activate the window so it paints.
2. Run `_recover_to_dashboard.ahk <STORE>` ONCE; check `logs/_recover_result.txt`. If FAIL, wait, fix the specific screen — do NOT loop logins rapidly.
3. Once on a verified dashboard (Reports sidebar present), it stays logged in for normal watcher cycling (Resume Session, not fresh login).


## 📋 RUN HISTORY — 2026-06-12 (intake-detail prestage)
- intake-detail (Claude Pawn Walks) for 2026-06-11: CUL+HAR+ROA succeeded; LEX & WAY failed 3 consecutive runs (incl. after _restart_watcher.ps1). Errors: LEX EnsureStore failed (2x) / Could not set Start Date (1x); WAY Could not set Start Date (3x). Bravo Status: Running throughout — NOT a wedge. Diag dump shows report screen open (Show summary panel checkbox visible) but date control not found. Suspect IntakeDetail.ahk date-picker UIA selector fails on LEX/WAY report screens. Logs: intake-detail-2026-06-12T06-37-05 / 06-45-43 / 06-50-58 / 06-56-59. Joshua DMed.

## ✅ FIXES SHIPPED 2026-06-12 (intake-detail)
1. reports/IntakeDetail.ahk (backup .bak-pre-datefallback-2026-06-12): added IntakeSetDateByPopupEditor fallback — on stores where the loaded report's date criteria cells expose as PopupBaseEdit (PART_Editor) instead of BravoDateEdit (observed LEX/WAY), set them by x-position via ValuePattern. PROVEN: WAY now succeeds (19 rows).
2. Same file: IntakeCloseReportScreen() in the error path — a failed cell now closes the Loans/Buys screen before returning. Root cause of the EnsureStore cascade: leftover open screen triggers Bravo warning 'Cannot switch stores: FREE1 is busy with Loans/Buys' (screenshot intake-detail-2026-06-12T06-56-59_LEX_switchstore-store-row.png).
3. Same file: saved-report load verification via BoxReportName after selection (soft gate; BoxReportName reads '' on LEX so it cannot hard-verify there).

## ⚠️ OPEN ISSUE 2026-06-12 — 'Claude Pawn Walks' saved report is WRONG ON LEX
The saved report named 'Claude Pawn Walks' loads with a Loan-Reviews-style definition at LEX (columns Ticket Number/Disposition/Disposition Date/Due Date/Pull Date/Customer/Loan Amount/Age/MobilePawn, criteria 'Age =', no Transaction Date filter) → export is unfiltered garbage (250-row cap hit). CUL/HAR/ROA have the correct 4-column definition; WAY has a 6-column variant (extra Ticket Kind + Associate) that still parses fine. These are per-store variants, NOT one global definition. FIX NEEDED IN BRAVO AT LEX (manual, ~2 min): Loans/Buys -> Custom Reports -> load 'Claude Pawn Walks' -> columns Ticket Number, Category, Full Description, Loan Amount; criteria Transaction Date between -> Save. Until then LEX intake-detail cells produce bad CSVs (quarantined as .bad-wrong-report in output/).

## ⚠️ UPDATE 2026-06-12 (evening) — LEX 'Claude Pawn Walks' REPORT rebuilt + VERIFIED, but list-view EXPORT still wrong
Joshua-approved automated rebuild (_lex_pawnwalks_rebuild3.ahk) SUCCEEDED at the dialog level: saved report now has BoxColumns=Full description and cost (490f2277), criteria Disposition Date range + Ticket Kind=BUY. Reload-verify confirmed all three in-dialog (SAVED+VERIFIED).
BUT: when the daily handler loads the report and clicks Ok, the LEX list-view grid still renders the 10-column layout (Ticket Number/Disposition/Disposition Date/Due Date/Pull Date/Customer/Loan Amount/Age/MobilePawn/SMS) AND is UNFILTERED (CSV had 56 BUY + 194 ON LOAN across many dates, 250-row cap). So the report DEFINITION is fixed but the per-store LIST-VIEW 'Saved Layouts' selection is independent of the report's BoxColumns and is still the wrong 10-col layout on LEX. CUL/HAR/ROA render the correct 4-col layout because their list-view Saved Layout is already the right one.
Automation could NOT open the list-view 'Saved Layouts' dropdown ('layout option not visible after opening combo' — _lex_fix_layout.ahk). Likely needs a human: at LEX, Loans/Buys -> Custom Reports -> run Claude Pawn Walks -> in the list view, Layouts panel -> Saved Layouts -> pick the 4-column 'Full description and cost' layout (the one CUL uses) -> Save Layout. THEN re-run intake-detail for LEX to confirm 4-col BUY-only output.
Also shipped: IntakeDetail.ahk patch4 (.bak-pre-nodblclick-2026-06-12) — removed the double-click commit branch in saved-report selection (it half-loaded the dialog); click->Enter only now.
Bad LEX CSVs quarantined in output/ as .bad-wrong-report*, .bad-listview-layout.

## RUN HISTORY -- 2026-06-16 (intake-detail prestage) -- intake-detail (Claude Pawn Walks) for 2026-06-15: run completed status=success, all 5 store CSVs written, Bravo Running throughout (NOT wedged, no restart needed). BUT all 5 stores (CUL,HAR,LEX,ROA,WAY) exported the WRONG 10-col loan-review layout (Ticket Number,Disposition,Disposition Date,Due Date,Pull Date,Customer,Loan Amount,Age,MobilePawn,SMS) -- no Category/Full Description col, and each hit the 250-row cap (unfiltered/truncated). REGRESSION: previously LEX-only (per 2026-06-12), now ALL FIVE. Not a watcher hang -- Bravo-side saved-report/list-view layout problem. Fix: per-store Loans/Buys -> Custom Reports -> run Claude Pawn Walks -> list view Layouts -> Saved Layouts -> 4-col Full description and cost -> Save Layout, then re-run. Joshua DMed. Run id intake-detail-2026-06-16T06-36-49.

## RUN HISTORY -- 2026-06-20 (intake-detail prestage)
intake-detail (Claude Pawn Walks) for 2026-06-19: FAILED 10/10 cells across TWO runs (ids intake-detail-2026-06-20T06-36-24 and -06-44-13), all 5 stores, status=partial, 0 CSVs. Every cell errored: "SelectSavedReport: could not select 'Claude Pawn Walks' via click or keyboard walk" -> combobox found at y=983 but "ClickByName 'Claude Pawn Walks': element not found". Bravo Running/responsive throughout (clean store switches, BackToDashboard OK) -- NOT a wedge, no restart done (healthy Bravo not cycled). Regression from earlier 'wrong-layout' issues: now the saved report isn't even present/selectable in the Custom Reports dropdown on ALL 5 stores. Needs manual Bravo-side fix: per store Loans/Buys -> Custom Reports -> confirm/re-save 'Claude Pawn Walks' so it appears in the saved-report combobox. Joshua DMed.

## RUN HISTORY -- 2026-07-10 (PAWN WALK / intake-detail)
intake-detail (Claude Pawn Walks) for 2026-07-09: FAILED 5/5 cells (CUL,HAR,LEX,ROA,WAY), status=partial, 0 CSVs. Every cell errored identically after 3 in-run re-selection attempts each: "Claude Pawn Walks did not load after 3 selection attempts (wrong report / no item columns)" -- log shows "step 4-5: skipping date override (report uses Age=1)" on every store, meaning the saved report is loading a Loan-Reviews-style definition (Age= criteria) instead of the item-level Transaction-Date/Category/Full-Description/Loan-Amount definition, matching the 2026-06-16 and 2026-06-20 regressions. Bravo Running/responsive throughout, clean store switches CUL->HAR->LEX->ROA->WAY->cooldown, BackToDashboard OK every time -- confirmed NOT a wedge/hang, so no watcher restart was attempted (per TOP RULE, restart is not the fix for this class of issue). Total runtime ~24 min (5 stores x ~4.3 min each, 3 attempts x ~70s + cooldown). Regression note: memory previously recorded LEX as fixed/clean on 2026-06-13 and 2026-07-03 -- this run shows the wrong-report issue has recurred and is now affecting ALL FIVE stores again, not just LEX. Needs manual Bravo-side fix per store: Loans/Buys -> Custom Reports -> open 'Claude Pawn Walks' -> confirm/re-save criteria as Transaction Date range (not Age=) with columns Ticket Number/Category/Full Description/Loan Amount, and confirm list-view Saved Layout is the 4-col 'Full description and cost' layout. Joshua DMed. Run id intake-detail-2026-07-10T07-23-24.

## CORRECTION 2026-07-10 (same session, ~20 min later)
Retracting the "manual re-save needed" framing above. Checked memory (reference-bravo-saved-reports-per-store, UPDATE 2026-07-06 entry): this EXACT signature -- all 5 stores, identical "wrong report / no item columns" after 3 attempts, log line "skipping date override (report uses Age=1)" -- was previously diagnosed as a FALSE FAILURE caused by an UNFIXED gate bug in IntakeDetail.ahk's column-verify logic: on a genuinely zero-row day, there are no DataItem rows to find FullDescription/Category AutoIds on, so a correct-but-empty grid is indistinguishable from a wrong layout and the gate retries then errors. That fix was still "pending Joshua go-ahead" as of 2026-07-06 -- i.e. NOT yet shipped. So 2026-07-09's identical 5/5 failure is MORE LIKELY this same known empty-day false-positive than a fresh saved-report corruption, though not confirmed (checked safe-register-journal CSVs for 7/9, which did land for all 5 stores and show routine safe-open/tender-transfer entries but aren't a reliable proxy for loan/buy volume). Recommend Joshua first check whether 7/9 was in fact a low-volume day at the stores before spending time re-saving "Claude Pawn Walks" -- and separately, greenlight the pending gate fix (distinguish zero-row-correct-layout from wrong-layout) so this stops false-failing on every quiet day.

## RUN HISTORY -- 2026-07-11 (PAWN WALK / intake-detail)
intake-detail (Claude Pawn Walks) for 2026-07-10: FAILED 5/5 cells (CUL,HAR,LEX,ROA,WAY), status=partial, 0 CSVs -- SECOND CONSECUTIVE DAY with the identical signature to 2026-07-10's run history entry (which covered 7/9 data): every cell errored "Claude Pawn Walks did not load after 3 selection attempts (wrong report / no item columns)" after 3 in-run re-selection attempts each, log shows "skipping date override (report uses Age=1)" on every store every attempt. Bravo Running/responsive throughout -- confirmed NOT a wedge (clean store switches CUL->HAR->LEX->ROA->WAY, BackToDashboard OK every time) -- so per TOP RULE no watcher/Bravo restart was attempted. Total runtime ~31 min. run_daily_intake.py ran clean (exit 0) but reported items=0 / info="No buys-from-public or intake-detail files found" -- this is a FALSE quiet-day read since 0 CSVs is due to the pull failure, not confirmed zero activity; treated as a failure, not a quiet day, and Joshua was DMed (not posted to #pawn-walks) per the failure-handling rule. Two days straight, all 5 stores, same signature strongly suggests this is NOT the isolated empty-day false-positive theorized in the 2026-07-10 correction (that theory assumed CSVs still get written just empty; here NO CSVs are written at all -- output_path empty on every cell) -- more likely the report-selection regression (parallel to 2026-06-16/2026-06-20 history) has recurred pipeline-wide. Recommend prioritizing the pending IntakeDetail.ahk gate fix or a manual per-store Bravo check over waiting for another quiet day to rule out. Run id intake-detail-2026-07-11T07-22-56.

## RUN HISTORY -- 2026-07-23 (PAWN WALK)
intake-detail (Claude Pawn Walks) for 2026-07-22: FAILED before any CSVs landed. Health gate (bravo_ensure_healthy.sh CUL) ran a full self-heal -- Rung3 relaunch, Rung4 recover-to-dashboard attempt 1 FAIL no-window, attempt 2 FAIL no-window, Rung4b ESCALATE guarded-kill (genuine hang, no ClickOnce activity) + relaunch, Rung4b recover attempt 1 FAIL no-window, attempt 2 -- overall gate result FAIL no-dashboard after ~9.5 min. Proceeded per policy to drop trigger anyway (intake-detail-2026-07-23T07-22-49): claimed by watcher at 07:34:41, watcher's own EnsureBravoDashboard waited up to 120s for Bravo, TRIPPED bravo-not-ready at 07:36:43, all 5 stores skipped, overall status=aborted, 0 CSVs. Ran ONE additional relaunch cycle beyond the health gate per STEP 4b (_relaunch_bravo_and_watcher.ps1) -- watcher restarted cleanly (watcher.last_started.txt advanced to 07:37:49, two new AHK PIDs), but Bravo.exe itself NEVER appeared in tasklist even after ~6 min wait (checked repeatedly; dfsvc.exe -- the ClickOnce launcher host -- also never appeared). This matches the 2026-07-21 known failure mode (Bravo.exe never relaunches at all -- not a UI wedge, not a wrong-report issue, not a login bounce). Stopped here per no-hammering-logins rule (already 3 relaunch cycles: health-gate x2 + this one). NOT run: run_daily_intake.py (would have produced a FALSE quiet-day read on 0 CSVs, matching the known 2026-07-11 false-quiet-day trap -- correctly treated as failure instead). Joshua DMed per v2 failure policy (plain one-liner only). NEXT SESSION: investigate why Bravo.exe / dfsvc.exe won't launch via the .appref-ms ClickOnce path at all from prlctl-driven PowerShell -- check ClickOnce trust/cache state, Windows Event Log for the Bravo.appref-ms launch attempt, and whether a stuck dfsvc.exe process (not visible via tasklist filter) is blocking new launches. This is now the SECOND occurrence (7/21, 7/23) of total non-launch -- likely needs a human to check the VM desktop directly via Parallels/computer-use (outside this pipeline's no-Parallels design) to see what's actually on screen.

## RUN HISTORY -- 2026-07-28 (PAWN WALK)
intake-detail (Claude Pawn Walks) for 2026-07-27: CUL/HAR/LEX/ROA all SUCCEEDED cleanly (13/12/21/30 rows, correct item-level layout, health gate PASS on first check, no restart needed for those 4). WAY FAILED: mid-run UIA error "GetBravoRoot: Bravo window not found" (after HAR/LEX/ROA store switches completed fine, crashed/lost window specifically entering WAY). Retried WAY alone -- second attempt also failed, EnsureBravoDashboard waited full 120s then "Bravo window never appeared", TRIPPED bravo-not-ready, status=aborted. Ran ONE _restart_watcher.ps1 cycle (watcher.last_started.txt advanced to 07:35:44) per STEP 4b, waited ~90s, re-dropped a fresh WAY-only trigger -- SAME failure ("no Bravo window yet" -> "never appeared" -> aborted). tasklist showed Bravo.exe running (2 PIDs, both Window Title: N/A) throughout both attempts -- process alive but no window UIA can attach to, distinct from the 7/21/7/23 "Bravo.exe never appears in tasklist at all" signature but same practical symptom (no reachable dashboard). Stopped after 1 relaunch cycle per no-hammering-logins rule. Compiled + posted margin summary for the 4 successful stores (76 items company-wide, 2 flags) via manual Slack post (run_daily_intake.py Slack token still dead, token_not_found -- 3rd+ recurrence, see reference-pawnwalk-compile-slack-token-dead memory). Joshua DMed re: WAY gap + flags. THIS IS THE THIRD DISTINCT OCCURRENCE of PAWN WALK losing Bravo mid-run/relaunch (7/21, 7/23, 7/27-data/7/28-run) -- recommend a human check the VM desktop directly via Parallels since headless recovery keeps failing for this specific fault mode.


## RUN HISTORY -- 2026-07-29 (PAWN WALK)
intake-detail (Claude Pawn Walks) for 2026-07-28: CUL/HAR/LEX/ROA all SUCCEEDED cleanly (7/12/14/32 rows, correct item-level layout, health gate PASS on first check <20s, no restart needed). WAY FAILED first attempt with a NEW error signature: EnsureStore store-row mismatch ("none of these store row Names matched: Waynesboro, WAYNESBORO, WAY, WAY") -- cause=store-row, explicitly NOT a lockout risk, breaker not incremented. Retried WAY alone via a fresh single-store trigger -- claimed immediately by watcher, but then EnsureBravoDashboard logged "no Bravo window yet - waiting up to 120s for (re)launch" and NEVER progressed past that line even ~5+ min later (log timestamp frozen at 07:32:28) -- i.e. Bravo window lost entirely on the very next attempt, matching the 7/21, 7/23, 7/27/28 window-loss signature. Did NOT attempt a watcher/Bravo relaunch cycle (already used the one allowed retry per no-hammering-logins rule) -- stopped and proceeded with 4/5 stores. Compiled + posted margin summary for the 4 successful stores via manual Slack post (run_daily_intake.py Slack token still dead, token_not_found -- see reference-pawnwalk-compile-slack-token-dead memory): 65 items company-wide, avg margin 55%, 7 flags. Joshua DMed re: WAY gap + flags. THIS IS THE FOURTH DISTINCT OCCURRENCE of PAWN WALK losing the WAY store specifically (7/21, 7/23, 7/27-data/7/28-run, now 7/28-data/7/29-run) -- and notably the failure MODE varies each time (store-row mismatch -> window loss) which suggests WAY's Bravo client/window is generally less stable than the other 4 stores, not a single reproducible bug. Recommend a human check the WAY store's Bravo session/VM window directly -- headless recovery has now failed 3+ times running for this specific store.

## RUN HISTORY -- 2026-07-31 (PAWN WALK)
intake-detail (Claude Pawn Walks) for 2026-07-30: CUL/HAR/LEX/ROA all SUCCEEDED cleanly (15/12/11/35 rows, correct item-level layout, health gate PASS on first check <20s, no restart needed for those 4). WAY FAILED: same 'GetBravoRoot: Bravo window not found' / ClickByName element-not-found signature as 7/27-7/29. Ran ONE _restart_watcher.ps1 cycle per STEP 4b, waited ~90s, re-dropped a WAY-only trigger -- froze at 'EnsureBravoDashboard: no Bravo window yet - waiting up to 120s for (re)launch' with zero progress, matching the exact 7/28/7/29 frozen signature (never even reaches the 120s abort in reasonable time). Stopped after 1 relaunch cycle per no-hammering-logins rule. Compiled + posted margin summary for the 4 successful stores manually via Slack MCP (run_daily_intake.py Slack token still dead, token_not_found -- see reference-pawnwalk-compile-slack-token-dead memory): 73 items company-wide, avg margin 58%, 8 flags. Joshua DMed re: WAY gap + flags. THIS IS THE FIFTH DISTINCT OCCURRENCE of PAWN WALK losing the WAY store specifically (7/21, 7/23, 7/27, 7/28, now 7/29-data/7/31-run) -- headless recovery has never once succeeded for this fault mode across 3+ attempts. Strongly recommend a human check the WAY store's Bravo session/VM window directly via Parallels rather than continuing to spend ~10 extra min per run on a retry that has a 0% success rate so far.


## RUN HISTORY -- 2026-08-02 (PAWN WALK)
intake-detail (Claude Pawn Walks) for 2026-08-01: health gate PASSED CUL on first check (<35s, no restart needed for Bravo itself -- confirmed Running/logged-in, LEX store title visible). First trigger (intake-detail-2026-08-02T10-50-23) sat UNCLAIMED in triggers/ for 3+ min -- never moved to triggers/claimed/. Per STEP 4b, ran ONE _restart_watcher.ps1 cycle -- restart script completed cleanly (old PID 11948 stopped, new PID 17092 confirmed Running via tasklist), but logs/watcher.last_started.txt did NOT advance (stayed at Aug 1 08:54:19 stale timestamp -- consistent with reference-watcher-last-started-stale memory, so verified recovery via live tasklist PID instead, not the stale file). Re-dropped fresh trigger (intake-detail-2026-08-02T10-57-05) -- STILL UNCLAIMED after 7+ min of polling post-restart. Confirmed via tasklist: both AutoHotkey64.exe (PID 17092, watcher script) AND Bravo.exe (PID 9164, window title 'VALLEY PAWN - LEXINGTON (LEX)') alive and Status=Running throughout -- NOT a Bravo-window-loss case (unlike 7/21,7/23,7/27-7/31 WAY-specific history) and NOT an unclaimed-watcher-dead case (process confirmed alive). This is a NEW failure signature: watcher process alive but its trigger-claim loop appears non-functional post-restart -- CPU time barely advanced (2s->3s over ~10 min), suggesting the watcher's polling timer may not be firing even though the process itself is running. No result.json ever produced. Stopped after 1 relaunch cycle per no-hammering-logins rule -- did NOT attempt a second restart or Parallels GUI recovery (out of scope for this pipeline). NOT run: run_daily_intake.py (0 CSVs landed, would be a false read). Joshua DMed per v2 failure policy (plain one-liner only, D03BHQH5VGT). NEXT SESSION: investigate whether _restart_watcher.ps1's kill/relaunch sequence is leaving the new AHK process in a state where its trigger-polling timer/loop never actually starts (process exists, window exists, but no claim activity) -- check bravo_watcher.ahk's OnInit/timer-registration logic and whether _restart_watcher.ps1's PID-kill timing races the new process's startup. Consider adding a watcher self-test (e.g. drop a synthetic trigger right after restart and verify claim within 60s) to the restart script itself so this class of silent-failure is caught before triggers are dropped for real.


## CORRECTION 2026-08-02 (same session, ~20 min later)
Retracting the 'watcher poll loop non-functional' framing in the entry directly above. What actually happened: the original watcher (PID 11948, alive since prior day) HAD genuinely gone stale. The _restart_watcher.ps1 recovery correctly fixed it (new PID 17092) -- but claim took ~4 min post-restart because the new watcher found Bravo auto-locked to a login screen and had to RecoverFromAutoLock before it could process. Full 5-store pull then completed successfully in ~10 min total (log: intake-detail-2026-08-02T10-50-23.log, result: status=success, CUL 11 / HAR 7 / LEX 2 / ROA 7 / WAY 12 rows, finished 11:04:34). The agent gave up polling at ~7 min post-restart -- before the legitimate ~10-12 min full-run window elapsed -- wrongly concluded the watcher was broken, sent a false failure DM to Joshua, and (worse) dropped a second redundant trigger (intake-detail-2026-08-02T10-57-05) while the first was still mid-run; the second queued behind it and ran again as a harmless duplicate. Corrected DM sent to Joshua. LESSON: after a watcher restart, do not declare failure before ~12-15 min -- check the run's own log file (logs/<trigger-id>.log) for live progress (store switches, row counts) rather than relying only on triggers/claimed/ timing, and never drop a second trigger without first checking whether an earlier trigger's log shows it's still actively running.
## RUN HISTORY -- 2026-08-04 (PAWN WALK)
intake-detail (Claude Pawn Walks) for 2026-08-03: CUL FAILED both the primary run and a single-store retry -- identical signature both times: "Claude Pawn Walks did not load after 3 selection attempts (wrong report / no item columns)", cause=nav, Bravo Running/responsive throughout (clean store landings, BackToDashboard OK) -- confirmed NOT a wedge, per TOP RULE no watcher/Bravo restart attempted, stopped after the one allowed retry. HAR/LEX/ROA/WAY all reported UIA-level SUCCESS with correct item-detail columns confirmed ("item-detail columns present in grid (FullDescription/Category)") and real row counts (45/46/30/44) -- first fully clean WAY run in weeks (no window-loss).

BUT: run_daily_intake.py compiled to items=0 / info="No buys-from-public or intake-detail files found" -- a FALSE quiet-day read. Verified by hand: the 4 successful CSVs are NOT empty and DO have the right header (Ticket Number,Category,Full Description,Disposition,Disposition Date,Due Date,Pull Date,Customer,Loan Amount,Age,MobilePawn,SMS,Last Payment) -- but every row inspected has Disposition=ON LOAN with Age in the 40s (e.g. Age=46, Disposition Date 6/19/2026, Pull Date 8/4/2026) and ZERO rows have Disposition=BUY across all 4 stores (grep -c ',BUY,' = 0 for HAR/LEX/ROA/WAY). This means the 'Claude Pawn Walks' saved report's underlying Age=1 / yesterday-only criteria is NOT actually being applied at the Bravo report-definition level on these 4 stores right now -- it's returning a broad Active-Loans-by-age list instead of yesterday's new intake, so run_daily_intake.py's Disposition Date == target-date filter (correctly) zeroed everything out. This is functionally the same class of issue as reference-pawnwalk-age-criteria-false-pass but WORSE than previously seen: earlier the concern was the gate marking a genuinely-empty grid as pass; here the grid is full of real rows but they're the WRONG rows (old loans, not new intake), so neither items=0 nor a nonzero row count from the UIA layer is trustworthy right now without checking Disposition/Disposition Date.

Compound with CUL's total report-load failure: 0/5 stores produced usable new-intake data for 2026-08-03. Treated as a FAILURE, not a quiet day -- Joshua DMed per policy (plain one-liner, D03BHQH5VGT), nothing posted to #pawn-walks.

NEXT SESSION: (1) Bravo-side, check the 'Claude Pawn Walks' saved report definition on HAR/LEX/ROA/WAY -- criteria likely drifted off Age=1 (or whatever yesterday-scoping criteria it should use) to a broader Active Loans filter; CUL's saved report is failing to load at all (3rd store now, after LEX historically and per intermittent all-store regressions in June/July -- may be worth a full per-store manual re-save + verify pass on all 5 rather than chasing one store at a time). (2) Consider hardening run_daily_intake.py's console output to explicitly log 'N rows read, 0 after BUY/ON LOAN+date filter' vs the current 'no files found' framing, since the current INFO message is misleading when files exist but are the wrong report scope -- makes future false-quiet-day debugging slower than it needs to be.

## RUN HISTORY -- 2026-08-06 (PAWN WALK)
intake-detail (Claude Pawn Walks) for 2026-08-05: FAILED 5/5 cells on BOTH the primary run (trigger intake-detail-2026-08-06T07-23-28) and a full 5-store retry after one watcher-restart recovery cycle (trigger intake-detail-2026-08-06T07-34-21). Health gate PASS CUL on first check (~33s). Primary run: CUL failed 3x with 'wrong report loaded (no item cols)' (Age=1 signature), then HAR lost the Bravo window mid-recovery ('could not return to Dashboard between selection retries'), which cascaded into LEX/ROA/WAY all failing EnsureStore with 'Bravo not ready' -- 0 CSVs, status=partial. Checked Bravo via tasklist: Bravo.exe alive/Running but stuck on a generic 'Select a store' / Resume Session screen (not a full crash). Ran ONE _restart_watcher.ps1 cycle per STEP 4b -- watcher.last_started.txt advanced live (07:33:18), confirmed real (not stale). Re-dropped a fresh trigger: watcher correctly self-recovered via StoreSelect -> RecoverFromAutoLock -> Resume Session -> landed on CUL dashboard cleanly. BUT the retry run then failed ALL 5 STORES with the IDENTICAL signature: 'Claude Pawn Walks did not load after 3 selection attempts (wrong report / no item columns)', log line 'skipping date override (report uses Age=1)' on every store every attempt (CUL/HAR/LEX/ROA/WAY, ~230-260s per cell, 3 attempts each). Bravo Running/responsive throughout this second run -- clean store switches confirmed for HAR/LEX/ROA/WAY (store confirmed: X logged each time), so this was NOT a wedge/window-loss on the retry -- it is now a confirmed Bravo-side saved-report DEFINITION problem hitting ALL FIVE stores simultaneously, worse than the historical CUL/LEX-only or the 2026-08-03 HAR/LEX/ROA/WAY-only occurrences -- this is the first time CUL *and* all four others failed identically in the same run with 0/5 stores producing any CSV at all. Stopped after the one allowed relaunch cycle per no-hammering-logins rule. Did NOT run run_daily_intake.py (0 CSVs would be a false quiet-day read per reference-pawnwalk-age-criteria-false-pass). Joshua DMed per v2 failure policy (plain one-liner, D03BHQH5VGT). Nothing posted to #pawn-walks.
NEXT SESSION: this now looks like a genuine, current drift of the 'Claude Pawn Walks' saved report definition/criteria on ALL FIVE stores (still showing Age=1 / no item columns instead of the Transaction-Date+Category+FullDescription+LoanAmount item-level definition) rather than an isolated per-store issue -- likely needs a human to open Bravo directly (any store) and check/re-save the 'Claude Pawn Walks' custom report definition and its list-view Saved Layout, matching the manual fix steps documented in the 2026-06-12/2026-06-16 entries above, this time across all 5 stores rather than just LEX. Consider also checking whether a recent Bravo update or admin-side report sync reset the saved-report criteria company-wide.

## RUN HISTORY -- 2026-08-07 (PAWN WALK)
intake-detail (Claude Pawn Walks) for 2026-08-06: health gate FAILED no-dashboard after ~11 min (Rung3 relaunch -> Rung4 recover attempt1/2 FAIL no-window -> Rung4b guarded-kill genuine-hang force-kill -> relaunch -> Rung4b recover attempt1/2 FAIL no-window). Per policy proceeded to drop trigger anyway (intake-detail-2026-08-07T07-34-06) -- claimed immediately, but EnsureBravoDashboard waited full 120s, Bravo window never appeared, TRIPPED bravo-not-ready, all 5 stores skipped, status=aborted, 0 CSVs. Ran the one allowed additional relaunch cycle (_relaunch_bravo_and_watcher.ps1) beyond the health gate per STEP 4b -- script completed cleanly, watcher.last_started.txt advanced live to 07:37:20 (confirmed real, not stale), waited ~2 min settle. Re-dropped fresh trigger (intake-detail-2026-08-07T07-39-33) -- claimed immediately but IDENTICAL failure: 120s wait, Bravo window never appeared, TRIPPED bravo-not-ready, all 5 stores skipped, aborted, 0 CSVs. Confirmed via tasklist AFTER both failed attempts: Bravo.exe NOT running at all (No tasks are running which match the specified criteria) -- i.e. Bravo.exe genuinely never relaunched, not a window-attach/black-render issue. This is the exact Bravo.exe-never-appears-in-tasklist signature from 2026-07-21 and 2026-07-23 (dfsvc.exe/ClickOnce launch path). Stopped after 1 relaunch cycle per no-hammering-logins rule -- did NOT run run_daily_intake.py (0 CSVs would be a false quiet-day read per reference-pawnwalk-age-criteria-false-pass memory). Joshua DMed per v2 failure policy (plain one-liner only, D03BHQH5VGT). Nothing posted to #pawn-walks.

THIS IS NOW THE THIRD CONFIRMED OCCURRENCE of total Bravo.exe non-launch via the .appref-ms ClickOnce path from prlctl-driven PowerShell (7/21, 7/23, now 8/7) -- the NEXT SESSION item from 7/23 (investigate ClickOnce trust/cache state, Windows Event Log for the launch attempt, possible stuck dfsvc.exe blocking new launches) is still unaddressed and this failure mode is recurring on a pattern, not a one-off. Strongly recommend a human check the VM desktop directly via Parallels to see what is actually blocking the ClickOnce launch, since headless recovery has now failed 3 of 3 times for this specific fault.
