# Fleet Failure Ledger

Append-only. Every scheduled task writes ONE row here instead of DMing Joshua (Failure Policy v3, 2026-09-08). `fleet-guardian` reads OPEN rows at 12:45 and 21:45, re-runs what is in `rerun_manifest.json`, moves NEEDS_HUMAN=yes items to `Life OS/HUMAN_QUEUE.md`, then marks rows RECOVERED / COVERED / QUEUED / UNRECOVERED. Joshua gets at most one DM a day, from the guardian only.

| When (ET) | Task | What did not happen (plain) | Needs human? | Status |
|---|---|---|---|---|
| 2026-09-08 18:56 | gusto-keep-alive | Google SSO blocked by password challenge which cannot be bypassed without credentials. | yes, Joshua needs to cache Google credentials in browser or pass through login manually | QUEUED 2026-09-08 (matches existing HUMAN_QUEUE Gusto row, added 2026-09-08) |
| 2026-09-08 19:10 | zoom-voicemail-alert | Could not read the Zoom Phone admin Users/History pages — the zoom.us PBX admin app never surfaced readable content via Claude in Chrome (get_page_text found only canvas/no text, read_page returned only marketing-site nav/footer, repeated timeouts waiting on "document_idle"/extension side-panel); zoom.us/myhome loaded fine, so this looks specific to the PBX admin SPA's rendering, not a login/session issue. No call-history data was pulled this run, so no Slack alert was sent (correct fail-safe — did not guess). | NEEDS_HUMAN: no — or yes if this recurs: Joshua could try opening zoom.us/pbx/page/telephone/phoneUsers once in his own Chrome to confirm the page still renders normally for a human, which would point the issue at the extension/automation path rather than Zoom itself | UNRECOVERED 2026-09-08 (not in rerun_manifest.json rerun_safe list — default verify-only, guardian does not drive Chrome PBX admin SPA to retry; structural rendering issue, not a one-time fluke, so not treated as covered by zoom-voicemail-eod-review without confirmation) |
| 2026-09-08 21:30 | gusto-keep-alive | Google SSO password challenge could not be bypassed—Chrome lacks saved credentials and task rules prohibit typing passwords; identical to 18:56 run. | yes, cache Google password in Chrome or manually sign into app.gusto.com on Mac | QUEUED 2026-09-08 (matches existing HUMAN_QUEUE Gusto row, added 2026-09-08) |
| 2026-09-08 21:56 | vp-os-github-nightly-backup | Fleet-guardian recovery run committed 436 files clean of secrets (commit 7a161b0) and confirmed fast-forward, but `git push origin main` was blocked twice by the platform's own auto-mode action classifier ("Blocked by classifier"), not by GitHub auth — the commit exists locally only, not on the backup remote. | NEEDS_HUMAN: no — this is a platform permission gate, not something Joshua can fix by signing into anything; if it recurs nightly, worth a session checking whether git push needs an explicit Bash permission rule added in settings | UNRECOVERED 2026-09-08 (retried once per policy, same classifier block both times; commit is safe locally, will push next successful run since fast-forward is preserved) |
| 2026-09-08 22:05 | vp-website-trend-daily-refresh | GA4 site confirmed reachable and account-correct (authuser=1, no welcome/provision screen), but pulling all 5 time-horizon windows x 2 report types x manual table extraction, then rewriting the artifact DATA object, is a full multi-step session task, not a bounded recovery action — GA4's own report render alone took 15+ seconds per view. Guardian stopped per its own 10-minute-per-rerun bound rather than let one task consume the whole run's budget; no data was pulled or guessed. | NEEDS_HUMAN: no — this needs a dedicated Claude session to run the full vp-website-trend-daily-refresh SKILL.md end to end (GA4 has been stale ~9 days per file mtime), not a human action | UNRECOVERED 2026-09-08 (queued for a dedicated catch-up session, not another guardian pass — same task will still show MISSED next guardian run until that session completes) |
| 2026-09-09 02:54 | gusto-keep-alive | Google SSO account chooser unresponsive—jdavis@fcfpawn.com link did not navigate after clicks or double-clicks; page remained on accountchooser despite multiple retry attempts and waits. | yes, Joshua should verify Google account link is clickable in his own Chrome, or try manual sign-in to app.gusto.com to cache the session | OPEN |
