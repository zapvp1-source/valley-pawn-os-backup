# Deep KPI Analysis — Handoff 2026-05-15

## Delivered today

### Workbooks
1. **`Valley_Pawn_Buys_KPI_2024-05_to_2026-04.xlsx`** — full 24-month buys analysis across all 5 stores.
   - 22,346 line items, $1,824,929 capital deployed
   - 8 tabs: Executive Summary, By Store, By Category, Monthly Trend, Heat Map, Fringe Patterns, Recommendations, Raw Data
   - All analytical tabs use SUMIFS / COUNTIFS / MAXIFS against the Raw Data tab — refreshes when source CSV updates

2. **`Valley_Pawn_GeneralMerch_Buys_KPI_2024-05_to_2026-04.xlsx`** — depreciation-risk focused workbook
   - Filters out firearms, jewelry, precious metals → 10,122 GM line items, $465,621
   - 9 tabs incl. Age Buckets tab (highlights items aged 12+ months)
   - Recommendation #1 is the inventory join (currently blocked — see below)

### Source CSVs (in this folder)
- `buys_master_ALLSTORES_2024-05-14_to_2026-04-30.csv` (combined)
- `buys_master_PERIODED_2024-05_to_2026-04.csv` (combined with Period + Chunk Start/End columns for time series)
- `buys_master_<STORE>_*.csv` × 5 (per-store)

### Infrastructure built
- `BuysFromPublic.ahk` handler in Bravo Data Extraction pipeline — proven, works reliably, supports date ranges and scrolling
- `extract_buys_chunked.py` — orchestrator that splits date ranges into monthly chunks, drops triggers sequentially, concatenates per-store CSVs
- `launch_watcher_in_session1_v2.py` — auto-restarts the watcher into the user's Session 1 (no manual click required)
- Scheduled task pattern for spawning AHK in Session 1 documented in memory

## Pending — Phase 8 (Buys × Inventory join)

Handlers written and registered but ONE step doesn't work:

### Status
- `ActiveInvDetails.ahk` — clones BuysFromPublic, navigates Inventory → Custom Reports → Choose Saved Report dropdown → select "Claude Active Inv Details" → ❌ blocked at "click Ok to run report"
- `SoldInvDetails.ahk` — same pattern, same blocker
- Both registered in `bravo_watcher.ahk` as `active-inv-details` and `sold-inv-details` handlers

### Blocker
The Inventory module's Custom Reports dialog stays open after my attempts to click Ok. Tried:
1. `ClickByName("Ok")` — found something named "Ok" but it didn't dismiss the dialog (likely hit a column header, not the dialog button)
2. `FindElement({AutomationId: "btnOk"}).InvokePattern.Invoke()` — btnOk wasn't visible in the AutoId tree at the time my code looked (within 20s of select)
3. `Send("{Enter}")` to fire the dialog's default action — also didn't dismiss

Diagnostic dumps show these buttons visible when the dialog is open:
- `Ok` (autoId=btnOk) — but only AFTER the dialog has been open for a while
- `Save` (no autoId)
- `New Report` (no autoId)
- `Cancel` (no autoId, separately from `Cancel` autoId=btnCancel)
- `Done` (autoId=btnDone)

### What's needed from Joshua
Two things — both blockers as of 2026-05-17:

1. **Bring Bravo to any store dashboard.** State got wedged from yesterday's failed iterations. Esc-spam and dialog-Cancel can't recover. 10 seconds of manual click on your end.

2. **Confirm the saved report dropdown entry.** Joshua said on 5/17: "its in the drop down Claude Inventory Details" — so I updated `saved_report_value` from "Claude Active Inv Details" / "Claude Sold Inv Details" to a single shared name `Claude Inventory Details` in both ActiveInvDetails.ahk and SoldInvDetails.ahk. If that's not the exact name, correct me — both handlers' `saved_report_value` constants are right at the top of each file.

3. **Identify the exact "run report" button** in the Inventory Custom Reports dialog. Tried Ok / btnOk / Enter — none ran the report. The dialog stays open. Likely the right action is Done / btnDone or possibly a Run/Preview button I haven't found. **5 minutes of pair-debugging** when you're at the screen will fix it.

### Updated 2026-05-17 (later)
- **Walker works**: `walk_open_inventory_grid.ahk` reliably extracts data from any opened Bravo Custom Reports grid via PageDown + Show More. Successfully extracted CUL (4,753 rows) and WAY (3,819 rows) for sold inventory.
- **`Claude Inventory Details` saved report identified** — it's actually a SOLD inventory report (every row has Status=SOLD). Has columns: Number, Status, Category, Description, Cost, Price, Last Sold Price, Date.
- **Ok button is a Text element, not a Button** — `MouseClick` on its bounding-rect center works. The `set_dates_and_run.ahk` script proves we can autonomously set Status Date range and click Ok.
- **InventoryDetails.ahk autonomous handler is 90% wired** — registered as `inventory-details` trigger. Successfully navigates store-switch → dashboard → Inventory → Custom Reports → dialog. BUT the "Choose Saved Report" dropdown won't open via any UIA technique tried: `combo.Click("left")`, `ExpandCollapsePattern.Expand()`, mouse-click on dropdown arrow at (right-edge -20px, center), Send `{F4}`, Send `!{Down}`. None open the dropdown items for selection.

### Theory on the auto-pull blocker
The "bottom-most BravoComboBox" my handler finds at y=1671 may not actually be the saved-report dropdown — could be a different combo (like the criteria selector). Need to verify visually:
- Open the Inventory Custom Reports dialog manually
- Look at what's at screen y≈1671 — is that the saved-report dropdown, or something else?
- If something else, identify the saved-report dropdown's actual position/AutoId

### Workable interim process
1. You manually open "Claude Inventory Details" in Bravo on the store you want
2. Set Status Date filter to whatever window you need (trailing 12 months recommended)
3. Click Ok, wait for grid
4. Tell me — I fire the walker. ~12-15 min per store.
5. Repeat for each store.

**This works today.** Auto-pull is the next iteration.

### CSVs delivered so far (2026-05-17)
- `sold_inv_CUL_2026-05-17.csv` — 4,753 sold rows (mixed date range, was Joshua's default — re-pull recommended for 12mo alignment)
- `sold_inv_WAY_2026-05-17.csv` — 3,819 sold rows (12mo: 5/17/2025-5/17/2026)
- HAR, LEX, ROA still pending — use the walker on each
- `Valley_Pawn_CUL_SoldInventory_Performance.xlsx` — performance workbook from CUL data

### After the blocker is resolved
1. Smoke test active inv on CUL → expect a few thousand rows of current inventory
2. Build a similar chunked-or-single-shot orchestrator for active + sold inv across all 5 stores
3. Build the **Performance Analysis workbook**: sell-through rate, days-to-sell, realized margin, gross-margin per category. Plus an **Aged-on-Shelf** worklist matching buys 12+ months old with items still in active inventory — the specific markdown-or-liquidate list.

## Open backlog
- Phase 6A: Schedule weekly buys review (rolling 7-day, Monday morning post)
- Phase 6B: Schedule monthly cumulative refresh (refresh main workbook on the 1st)
- Phase 1E: Refine "Claude Buy Reviews" — add Customer Name / Store / Employee columns to the saved report so we get those dimensions in future pulls
- Phase 7: Build LoanReviews handler (uses the existing "Claude Loan Reviews" saved report)
