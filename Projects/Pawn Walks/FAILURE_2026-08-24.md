PAWN WALK FAILURE — 2026-08-24
STATUS: intake-detail pull for 2026-08-23 FAILED after 3 full trigger cycles + 3 health-gate recoveries + 2 watcher restarts (~1hr wall time, 07:23-08:20).

ROOT CAUSE (best evidence): The 'Claude Pawn Walks' saved report in Bravo failed to load correctly (WARN: wrong report loaded - no item cols) on EVERY store attempted across all 3 trigger runs (CUL, HAR, LEX, ROA all failed 3x each in the final run; WAY wedged mid-attempt-1 and never resolved). Zero intake-detail CSVs were written to output/ for 2026-08-23 across the entire session.

SECONDARY ISSUE OBSERVED: Bravo window title repeatedly desynced from the automation's believed current store (e.g. automation believed it had switched to HAR/WAY while the window title still showed CUL/ROA) — consistent with the known 'login bounce' issue in BRAVO_KNOWN_ISSUES.md. This likely explains why the saved report combo box, though selected correctly, rendered the wrong screen/no item columns.

ALSO NOTE (self-inflicted, logged for transparency): a duplicate trigger was dropped early in this run (~07:31) while the first trigger (07:23) was still executing, causing two concurrent automation runs on the single Bravo session — this contributed to at least one of the three stalls but is NOT the root cause of the final run's failure, since the final run (07:57, single trigger, fresh watcher) failed identically with no concurrency.

RECOVERY ACTIONS TAKEN (in order): (1) bravo_ensure_healthy.sh x3 — all PASSed (recovered to dashboard each time); (2) _restart_watcher.ps1 x2 — both completed and watcher resumed claiming triggers; (3) 3 full trigger drops (07:23, 07:31 dup, 07:44, 07:57) — all failed to produce a single CSV.

NEXT STEPS FOR NEXT SESSION: (a) verify the 'Claude Pawn Walks' saved report definition in Bravo hasn't been altered/corrupted (open it manually and confirm item-detail columns are present); (b) check whether a Bravo update/version change (window title showed '2026.6.0.79') altered report-load timing or the UIA selectors IntakeDetail.ahk uses to detect success; (c) consider increasing the wrong-report-loaded wait threshold if Bravo is simply slower to render than the ~30s check assumes; (d) yesterday's buys/loans data (2026-08-23) has NOT been pulled — no compile, no Slack post, no email were sent for this run per the failure policy.

Per Failure Alert Policy: DM already sent to Joshua (D03BHQH5VGT), plain language only. NOTHING posted to #pawn-walks channel — correct per policy (compile failures / pull failures are DM-only, never a channel post).
